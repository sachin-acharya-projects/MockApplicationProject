// Value can be obtained from Google Console

type TAuthClient = {
    web: {
        client_id: string,
        client_secret: string,
    },
    android: {
        client_id: string,
    },
}

export const authClient: TAuthClient = {
    
}